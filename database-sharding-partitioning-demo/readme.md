# Sharding:
* Run 'docker/sharding/run_databases.sh' to create multiple database servers using docker.
* Run '/src/main/java/com/hr/database/sharding/partitioning/demo/ShardingDemoApp.java' to see how records are inserted into and retrieved from different shards.

